//Acciones q va a lanzar el componente cuando se mande a llamar
//para q el AppStore haga los cambios correspondientes
import Reflux from 'reflux';

const Actions = Reflux.createActions([
  ''
]);

export default Actions;
